services:
  app:
    build:
      context: .
      target: base
    command: ["python", "-m", "app.main"]
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./app:/app/app
      - ./secure/authorized_key.json:/secure/authorized_key.json:ro
      - ./secure/google-credentials.json:/secure/google-credentials.json:ro
      - ./tests:/app/tests
    restart: unless-stopped

  scheduler:
    build:
      context: .
      target: base
    command: ["python", "-m", "app.scheduler"]
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./app:/app/app
      - ./secure/authorized_key.json:/secure/authorized_key.json:ro
      - ./secure/google-credentials.json:/secure/google-credentials.json:ro
    restart: unless-stopped

  worker:
    build:
      context: .
      target: base
    command: ["python", "-m", "app.worker"]
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./app:/app/app
      - ./secure/authorized_key.json:/secure/authorized_key.json:ro
      - ./secure/google-credentials.json:/secure/google-credentials.json:ro
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_EXPOSE_PORT:-5432}:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--save", "60", "1", "--loglevel", "notice"]
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

volumes:
  pg_data:
  redis_data:
